export interface Profile {
    id: number;
    username: string;
    email: string;
    created_at: Date;
    updated_at: Date;
}
export interface updatedUser {
    username: string;
    email: string;
}